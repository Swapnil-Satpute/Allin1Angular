import { AutoDirectiveDirective } from './auto-directive.directive';

describe('AutoDirectiveDirective', () => {
  it('should create an instance', () => {
   // const directive = new AutoDirectiveDirective();
   // expect(directive).toBeTruthy();
  });
});
